[project]
name = "fly-in"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "arcade>=3.3,<4",
]

[dependency-groups]
dev = [
    "flake8>=7.3.0",
    "mypy>=2.3.0",
    "pytest>=9.1.1",
]
